services:
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    container_name: hockey-card-pricer-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env